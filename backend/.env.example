# Server Configuration
PORT=5000
NODE_ENV=production

# Database (Get this from MongoDB Atlas)
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/timesheet?retryWrites=true&w=majority

# Security (Change this to a random long string)
JWT_SECRET=your_secure_random_string_here
